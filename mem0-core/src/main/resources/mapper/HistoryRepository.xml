<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mem0.core.repository.HistoryRepository">

    <resultMap id="HistoryResultMap" type="com.mem0.core.domain.model.MemoryHistory">
        <id property="id" column="id"/>
        <result property="memoryId" column="memory_id"/>
        <result property="oldMemory" column="old_memory"/>
        <result property="newMemory" column="new_memory"/>
        <result property="event" column="event"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="actorId" column="actor_id"/>
        <result property="role" column="role"/>
    </resultMap>

    <resultMap id="HistoryAsMapResultMap" type="java.util.LinkedHashMap">
        <id property="id" column="id" javaType="String"/>
        <result property="memory_id" column="memory_id" javaType="String"/>
        <result property="old_memory" column="old_memory" javaType="String"/>
        <result property="new_memory" column="new_memory" javaType="String"/>
        <result property="event" column="event" javaType="String"/>
        <result property="created_at" column="created_at" javaType="String"/>
        <result property="updated_at" column="updated_at" javaType="String"/>
        <result property="is_deleted" column="is_deleted" javaType="Boolean"/>
        <result property="actor_id" column="actor_id" javaType="String"/>
        <result property="role" column="role" javaType="String"/>
    </resultMap>

    <select id="findByMemoryIdOrderByCreatedAtAsc" resultMap="HistoryResultMap">
        SELECT id, memory_id, old_memory, new_memory, event,
               created_at, updated_at, is_deleted, actor_id, role
        FROM history
        WHERE memory_id = #{memoryId}
        ORDER BY created_at ASC, updated_at ASC
    </select>

    <select id="findByMemoryIdAsMap" resultMap="HistoryAsMapResultMap">
        SELECT id, memory_id, old_memory, new_memory, event,
               created_at, updated_at, is_deleted, actor_id, role
        FROM history
        WHERE memory_id = #{memoryId}
        ORDER BY created_at ASC, updated_at ASC
    </select>

    <insert id="insert" parameterType="com.mem0.core.domain.model.MemoryHistory">
        INSERT INTO history (id, memory_id, old_memory, new_memory, event,
                             created_at, updated_at, is_deleted, actor_id, role)
        VALUES (#{id}, #{memoryId}, #{oldMemory}, #{newMemory}, #{event},
                #{createdAt}, #{updatedAt}, #{isDeleted}, #{actorId}, #{role})
    </insert>

    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO history (id, memory_id, old_memory, new_memory, event,
                             created_at, updated_at, is_deleted, actor_id, role)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.memoryId}, #{item.oldMemory}, #{item.newMemory}, #{item.event},
             #{item.createdAt}, #{item.updatedAt}, #{item.isDeleted}, #{item.actorId}, #{item.role})
        </foreach>
    </insert>

    <delete id="deleteAll">
        DELETE FROM history
    </delete>

</mapper>
